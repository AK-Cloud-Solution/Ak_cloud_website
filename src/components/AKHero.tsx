import { ArrowDown, ArrowRight, Play, Sparkles } from "lucide-react";
import { heroStates } from "../content/landingContent";
import { SiteContent } from "../content/siteContent";

export function AKHero({ content, onPrimaryClick, onSecondaryClick }: { content: SiteContent["hero"]; onPrimaryClick: () => void; onSecondaryClick: () => void }) {
  return <section id="home" className="new-hero"><div className="hero-background cinematic-video"><video autoPlay muted loop playsInline preload="metadata" poster="/assets/akcloud-heygen-hero-poster.jpg" aria-label="A creative technology team working together in a modern office"><source src="/assets/akcloud-heygen-hero.mp4" type="video/mp4" /></video><div className="video-vignette" /><div className="hero-image-overlay" /></div><div className="hero-container"><div className="hero-content"><div className="hero-kicker"><span /> {content.eyebrow}</div><h1>{content.title}<em>{content.accent}</em></h1><p>{content.description}</p><div className="hero-actions"><button className="hero-primary" onClick={onPrimaryClick}>{content.primaryCta} <ArrowRight /></button><button className="hero-secondary" onClick={onSecondaryClick}><Play /> {content.secondaryCta}</button></div></div><div className="human-ai-overlay" aria-label="Live AK Cloud activity">{heroStates.map(({ text, icon: Icon }, i) => <div className={`activity-chip activity-${i + 1}`} key={text}><span><Icon /></span><div><small>AK CLOUD</small><b>{text}</b></div><i /></div>)}</div><div className="hero-caption"><Sparkles /><span><b>Quietly working in the background</b><small>6 connected workflows active</small></span></div></div><button className="hero-scroll" onClick={onSecondaryClick}><span>Discover the workspace</span><ArrowDown /></button></section>;
}
